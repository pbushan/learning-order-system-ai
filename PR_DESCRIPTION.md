## Agent-Generated CODEOWNERS Update

This pull request was generated by an LLM coding agent (Codex) and is intentionally scoped for human review.

## What I inspected

The agent inspected the repository state before making changes:

- Verified that `.github/CODEOWNERS` did not exist
- Confirmed the repository contains `order-api`, `order-consumer`, `order-pricing-lambda`, and `ai-experiments`
- Reviewed the existing PR helper script in `scripts/agent-pr.sh`

## Decision made

Because `CODEOWNERS` was missing, the agent created a new `.github/CODEOWNERS` file instead of modifying unrelated files. It added `@pbushan` as the default owner and added explicit ownership entries for the service directories. It also included `/ai-experiments/` because that directory exists in the repository and the repository policy in `AGENTS.md` calls for ownership there when present.

## What changed

- Created `.github/CODEOWNERS`
- Added `* @pbushan` as the default owner
- Added explicit ownership for:
  - `/order-api/`
  - `/order-consumer/`
  - `/order-pricing-lambda/`
  - `/ai-experiments/`
- Updated `scripts/agent-pr.sh` so it works with the required branch name and can create a PR after changes have already been committed

## Why this change is needed

`CODEOWNERS` provides a clear ownership model for repository review workflows. It helps route review responsibility, makes governance explicit, and reduces the risk of changes landing without the right human oversight.

## LLM disclosure

This PR was generated by Codex, an LLM-based coding agent. The agent inspected repository state, made a minimal change based on that state, and prepared this PR for human review and approval.

## Why PR-based governance matters

PR-based governance is important because it preserves a controlled workflow:

- agents can propose changes without writing directly to `main`
- humans retain final approval authority
- review history stays visible and auditable
- repository policy can be enforced consistently

This supports the operating model documented for this repository: AI proposes changes, and humans review and merge them.
